/*
 * pwix:iziam-oidc/src/common/js/constants.js
 */

izIAM.C = {
    Service: 'izIAM',

    // verbosity levels
    Verbose: {
        NONE: 0,
        CONFIGURE: 0x01 <<  0
    }
};

// non exported variables

I18N = 'pwix:iziam-oidc:i18n:namespace';
